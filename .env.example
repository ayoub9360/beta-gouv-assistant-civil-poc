# OpenAI
OPENAI_API_KEY=sk-proj-xxx
OPENAI_MODEL=gpt-4o

# CORS — domaines autorisés par le backend (séparés par des virgules si plusieurs)
ALLOWED_ORIGINS=http://localhost:3000

# URL publique du backend (utilisée par le frontend au build)
NEXT_PUBLIC_API_URL=http://localhost:8000
